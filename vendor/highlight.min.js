(function(){
  window.hljs = {
    highlightAll: function(){
      // Minimal stub: add basic class to <code> blocks for styling if needed
      document.querySelectorAll('pre code').forEach(function(block){
        // noop: keep code content as-is; real highlighting not included
        block.classList.add('hljs');
      });
    }
  };
})();
